/**
 * @typedef Range
 * @property {number} x The X range
 * @property {number} y The Y range
 */

/**
 * @typedef ZombieMovementRange
 * @property {Range} from The "from" range
 * @property {Range} to The "to" range
 */
